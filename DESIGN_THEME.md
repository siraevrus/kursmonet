# Дизайн-тема проекта Conversor

## 🎨 Общая информация

Проект использует **темную тему (Dark Theme)** с современным Material Design 3 и цветовой палитрой на основе Tailwind CSS Slate и Blue.

---

## 🌈 Цветовая палитра

### Основные цвета фона:

| Цвет | HEX | Описание | Использование |
|------|-----|----------|--------------|
| `backgroundApp` | `#020617` | Slate-950 (очень темный) | Фон всего приложения |
| `backgroundCard` | `#1E293B` | Slate-800 (темно-серый) | Карточки валют, кнопки |
| `backgroundHeader` | `#0F172A` | Slate-900 (темный) | AppBar, модальные окна |

### Акцентные цвета:

| Цвет | HEX | Описание | Использование |
|------|-----|----------|--------------|
| `accentPrimary` | `#2563EB` | Blue-600 (синий) | Активная валюта, индикаторы, кнопки действий |
| `deleteButton` | `#EF4444` | Red-500 (красный) | Кнопка удаления валюты |

### Цвета текста:

| Цвет | HEX | Описание | Использование |
|------|-----|----------|--------------|
| `textPrimary` | `#FFFFFF` | Белый | Основной текст, заголовки |
| `textSecondary` | `#94A3B8` | Slate-400 (светло-серый) | Вторичный текст, подсказки |

### Границы и разделители:

| Цвет | HEX | Описание | Использование |
|------|-----|----------|--------------|
| `dividerBorder` | `#334155` | Slate-700 (средне-серый) | Границы карточек, разделители |

---

## 📐 Типографика

### Шрифт:
- **Основной шрифт**: `Inter` (без засечек, современный)
- Используется для всех текстовых элементов

### Размеры и стили:

| Стиль | Размер | Вес | Использование |
|-------|--------|-----|---------------|
| `displayLarge` | 30px | Bold | Крупные заголовки |
| `displayMedium` | 24px | Bold | Заголовки карточек валют, суммы |
| `titleLarge` | 20px | Bold | Название приложения, коды валют |
| `bodyMedium` | 12px | Normal | Вторичный текст, подсказки, время обновления |

---

## 🎯 Компоненты дизайна

### 1. AppBar (Верхняя панель)
- **Фон**: `backgroundHeader` с прозрачностью 0.9
- **Цвет текста**: `textPrimary` (белый)
- **Высота**: стандартная Material Design
- **Элементы**: название приложения, время обновления, кнопка обновления

### 2. Карточки валют (CurrencyCard)
- **Фон**: `backgroundCard` (Slate-800)
- **Граница**: 
  - Обычная: `dividerBorder` (1px)
  - Активная: `accentPrimary` (1px, синяя)
- **Скругление**: 16px (borderRadius)
- **Отступы**: 16px по горизонтали, 8px по вертикали
- **Внутренние отступы**: 16px

### 3. Кнопка добавления валюты
- **Фон**: `backgroundCard`
- **Граница**: `dividerBorder` (1px, solid)
- **Скругление**: 16px
- **Отступы**: 24px внутренние, 16px внешние
- **Иконка**: `Icons.add` цвета `textSecondary`

### 4. Поле ввода (TextField)
- **Стиль**: без границ (InputBorder.none)
- **Цвет текста**: `textPrimary` (белый)
- **Размер**: 24px, Bold
- **Выравнивание**: по правому краю
- **Символ валюты**: справа, прозрачность 0.5

### 5. Модальное окно добавления валюты
- **Фон**: `backgroundHeader`
- **Скругление**: 20px сверху
- **Высота**: адаптивная (isScrollControlled)

### 6. Индикатор обновления (RefreshIndicator)
- **Цвет**: `accentPrimary` (синий)
- **Фон**: `backgroundCard`

---

## 🌍 Фоновое изображение

- **Изображение**: `assets/earth.png`
- **Прозрачность**: 0.15 (15%)
- **Режим**: `BoxFit.cover` (покрывает весь экран)
- **Расположение**: фон всего приложения

---

## 🎨 Material Design 3

Проект использует **Material Design 3** (`useMaterial3: true`), что обеспечивает:
- Современный внешний вид
- Плавные анимации
- Улучшенную типографику
- Современные компоненты

---

## 🌓 Режим отображения

- **Тема**: `Brightness.dark` (темная тема)
- **Яркость**: низкая (темные цвета)
- **Контрастность**: высокая (белый текст на темном фоне)

---

## 📱 Адаптивность

Дизайн адаптирован для:
- Различных размеров экранов
- Портретной и альбомной ориентации
- Разных плотностей пикселей

---

## 🎯 Принципы дизайна

1. **Минимализм**: чистый интерфейс без лишних элементов
2. **Контрастность**: четкое разделение элементов через цвет и границы
3. **Иерархия**: важные элементы выделены цветом и размером
4. **Читаемость**: высокий контраст текста и фона
5. **Современность**: использование Material Design 3 и актуальных цветовых решений

---

## 🔍 Примеры использования цветов

### Активная валюта:
```dart
border: Border.all(
  color: AppTheme.accentPrimary, // Синяя граница
  width: 1,
)
```

### Обычная валюта:
```dart
border: Border.all(
  color: AppTheme.dividerBorder, // Серая граница
  width: 1,
)
```

### Удаление валюты:
```dart
color: AppTheme.deleteButton, // Красный фон
```

### Вторичный текст:
```dart
color: AppTheme.textSecondary, // Светло-серый
```

---

## 📊 Визуальная иерархия

1. **Уровень 1** (самый важный): Активная валюта с синей границей
2. **Уровень 2**: Обычные карточки валют
3. **Уровень 3**: Вторичная информация (время обновления, курсы)
4. **Уровень 4**: Фоновые элементы (земля, разделители)

---

## 🎨 Цветовая схема в коде

```dart
class AppTheme {
  // Фоны
  static const Color backgroundApp = Color(0xFF020617);      // Основной фон
  static const Color backgroundCard = Color(0xFF1E293B);     // Карточки
  static const Color backgroundHeader = Color(0xFF0F172A);   // Заголовки
  
  // Акценты
  static const Color accentPrimary = Color(0xFF2563EB);      // Синий
  static const Color deleteButton = Color(0xFFEF4444);       // Красный
  
  // Текст
  static const Color textPrimary = Color(0xFFFFFFFF);       // Белый
  static const Color textSecondary = Color(0xFF94A3B8);      // Серый
  
  // Границы
  static const Color dividerBorder = Color(0xFF334155);     // Разделители
}
```

---

## 💡 Рекомендации по использованию

1. **Всегда используйте цвета из `AppTheme`** вместо хардкода
2. **Соблюдайте иерархию** при выборе цветов для элементов
3. **Используйте `textSecondary`** для второстепенной информации
4. **Применяйте `accentPrimary`** для активных элементов и действий
5. **Сохраняйте контрастность** для читаемости текста

---

*Тема разработана с учетом современных стандартов Material Design 3 и обеспечивает комфортное использование приложения в темное время суток.*






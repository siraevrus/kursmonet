# Проверка использования нового API

## Способ 1: Проверка логов при запуске приложения

### Через Flutter run:
```bash
flutter run
```

В логах ищите строку:
```
📡 [API] Запрос курсов валют: https://conversor.onza.me/api/rates?base=USD
```

**Ожидаемый URL:** `https://conversor.onza.me/api/rates`
**Старый URL (не должен появляться):** `https://api.fxratesapi.com/latest`

### Через adb logcat (для Android):
```bash
# Очистить логи
adb logcat -c

# Запустить приложение и смотреть логи
adb logcat | grep -E "API|conversor|fxratesapi"
```

Или используйте готовый скрипт:
```bash
./GET_APP_LOGS.sh
```

## Способ 2: Проверка формата ответа API

В логах проверьте формат ответа. Новый API возвращает:
```json
{
  "success": true,
  "base": "USD",
  "rates": {
    "EUR": {
      "rate": 0.85,
      "last_updated": "2024-01-15 12:00:00"
    }
  }
}
```

В логах должно быть:
```
✅ [API] Курсы получены успешно
   Базовая валюта API: USD
   Количество валют: XXX
```

## Способ 3: Прямой запрос к API (curl)

Проверьте, что новый API работает:

```bash
# Получить все курсы
curl "https://conversor.onza.me/api/rates?base=USD"

# Получить конкретный курс
curl "https://conversor.onza.me/api/rates?base=USD&target=EUR"

# Конвертация
curl "https://conversor.onza.me/api/convert?amount=100&from=USD&to=EUR"
```

**Ожидаемый ответ:**
- Поле `success: true`
- Поле `rates` содержит объекты с `rate` и `last_updated`
- Или для конкретного курса: `rate`, `target`, `last_updated`

## Способ 4: Проверка кода

Убедитесь, что в коде используется правильный URL:

```bash
grep -r "conversor.onza.me" lib/
grep -r "fxratesapi" lib/  # Не должно быть результатов
```

## Способ 5: Проверка через сетевой мониторинг

### Android Studio Network Inspector:
1. Запустите приложение через Android Studio
2. Откройте вкладку "Network Inspector"
3. Обновите курсы в приложении
4. Проверьте запросы - должен быть запрос к `conversor.onza.me`

### Charles Proxy / Proxyman:
1. Настройте прокси на устройстве
2. Запустите приложение
3. Обновите курсы
4. Проверьте запросы в прокси - должен быть запрос к `conversor.onza.me/api/rates`

## Способ 6: Проверка через тестовый запрос в коде

Добавьте временный тест для проверки:

```dart
// В main.dart или в тестовом файле
void testApi() async {
  final response = await CurrencyApiService.fetchRates();
  print('API URL: ${CurrencyApiService.baseUrl}');
  print('Response: $response');
}
```

## Что проверить:

✅ URL должен быть: `https://conversor.onza.me/api/rates`
❌ URL НЕ должен быть: `https://api.fxratesapi.com/latest`

✅ Ответ должен содержать поле `success: true`
✅ Курсы должны быть в формате объектов: `{"EUR": {"rate": 0.85, "last_updated": "..."}}`
❌ Курсы НЕ должны быть в формате чисел: `{"EUR": 0.85}`

✅ В логах должно быть: `✅ [API] Курсы получены успешно`
✅ Количество валют должно быть больше 0

## Быстрая проверка (одна команда):

```bash
# Запустить приложение и сразу проверить логи
flutter run & sleep 5 && adb logcat -d | grep -E "conversor.onza.me|fxratesapi" | head -20
```




# Команды для просмотра логов CurrencyPro

## Основные команды

### 1. Все логи Flutter приложения (рекомендуется):
```bash
adb logcat -s flutter
```

### 2. Логи с фильтром по меткам приложения:
```bash
adb logcat | grep -E "CURRENCY|CONVERSION|FOCUS|UPDATE|RATES|HIVE|API|AMOUNT|INIT|RECALC|APP|CURRENCY_MANAGEMENT"
```

### 3. Очистить логи и начать просмотр заново:
```bash
adb logcat -c && adb logcat -s flutter
```

### 4. Сохранить логи в файл:
```bash
adb logcat -s flutter > currency_pro_logs.txt
```

### 5. Просмотр логов в реальном времени с фильтром:
```bash
adb logcat | grep --line-buffered -E "flutter|CURRENCY|CONVERSION|FOCUS"
```

### 6. Только ошибки и предупреждения:
```bash
adb logcat *:E flutter:*
```

## Использование скрипта

В проекте есть готовый скрипт:
```bash
./GET_APP_LOGS.sh
```

## Через Flutter run

Если запускаете приложение через Flutter, логи видны прямо в терминале:
```bash
flutter run
```

## Примеры логов, которые вы увидите:

```
👆 [FOCUS] Получен фокус на валюте: EUR
🔄 [CURRENCY_SELECTION] Выбор базовой валюты: USD → EUR
🧮 [CONVERSION] Расчет конвертации:
   Формула: result = (amount / baseRate) * targetRate
```

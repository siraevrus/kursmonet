# CurrencyPro

Мобильное приложение для конвертации валют с автоматическим пересчетом в реальном времени.

## Особенности

- 🎨 Темная тема с современным дизайном
- 💱 Мультивалютный конвертер
- 🔄 Автоматическое обновление курсов
- 📱 Оффлайн режим с локальным кэшированием
- ⚡ Быстрая работа благодаря Hive
- 🎯 Интуитивный интерфейс
- 🔄 Перетаскивание для изменения порядка валют
- 🗑️ Свайп для удаления валют

## Технологии

- **Flutter** (Dart) - кроссплатформенный фреймворк
- **Riverpod** - управление состоянием
- **Hive** - локальное NoSQL хранилище
- **HTTP** - запросы к API
- **Cached Network Image** - кэширование изображений флагов
- **Intl** - форматирование чисел и валют

## Установка и запуск

### Требования

- Flutter SDK (>=3.0.0)
- Dart SDK
- Android Studio / Xcode (для эмуляторов)
- Интернет-соединение (для первого запуска)

### Шаги установки

1. **Клонируйте репозиторий или откройте проект**

2. **Установите зависимости:**
```bash
flutter pub get
```

3. **Запустите приложение:**
```bash
# Для Android
flutter run

# Для iOS (только на macOS)
flutter run

# Для конкретного устройства
flutter devices
flutter run -d <device_id>
```

### Первый запуск

При первом запуске приложение:
- Загрузит курсы валют из API
- Сохранит их в локальную базу данных (Hive)
- Установит дефолтные валюты: USD, EUR, RUB, KZT

## Использование

### Основные функции

1. **Конвертация валют:**
   - Нажмите на карточку валюты, чтобы сделать её базовой
   - Введите сумму в активной валюте
   - Остальные валюты автоматически пересчитаются

2. **Добавление валют:**
   - Нажмите кнопку "Добавить валюту" внизу списка
   - Выберите валюту из списка (поиск доступен)
   - Валюты разделены на "Популярные" и "Все"

3. **Удаление валют:**
   - Свайпните карточку валюты влево
   - Или откройте список валют и снимите галочку

4. **Изменение порядка:**
   - Зажмите и перетащите карточку валюты вверх/вниз

5. **Обновление курсов:**
   - Нажмите кнопку обновления в AppBar
   - Курсы обновляются автоматически при запуске

### Оффлайн режим

При отсутствии интернета:
- Приложение использует сохраненные курсы из кэша
- Отображается уведомление "Режим оффлайн"
- Все функции работают с последними загруженными данными

## Структура проекта

```
lib/
├── main.dart                    # Точка входа приложения
├── models/                      # Модели данных
│   └── currency_info.dart      # Информация о валютах (коды, названия, символы)
├── providers/                   # State Management (Riverpod)
│   └── currency_provider.dart  # Провайдер состояния валют и курсов
├── screens/                     # Экраны приложения
│   └── main_screen.dart         # Главный экран с списком валют
├── services/                    # Сервисы
│   ├── currency_api_service.dart  # Работа с API курсов валют
│   └── hive_service.dart       # Работа с локальной БД (Hive)
├── theme/                       # Тема приложения
│   └── app_theme.dart          # Цветовая палитра и стили
└── widgets/                     # Переиспользуемые виджеты
    ├── currency_card.dart       # Карточка валюты с полем ввода
    └── add_currency_sheet.dart  # Модальное окно выбора валют
```

## API

Приложение использует бесплатный API: **https://api.fxratesapi.com/latest**

### Формат ответа:
```json
{
  "base": "USD",
  "rates": {
    "EUR": 0.92,
    "RUB": 92.5,
    ...
  }
}
```

## Цветовая палитра

Приложение использует темную тему со следующими цветами:

- **Фон приложения**: `#020617` (Slate-950)
- **Фон карточек**: `#1E293B` (Slate-800)
- **Фон Header**: `#0F172A` (Slate-900)
- **Акцент**: `#2563EB` (Blue-600)
- **Текст основной**: `#FFFFFF` (Белый)
- **Текст вторичный**: `#94A3B8` (Slate-400)

## Особенности реализации

### Логика конвертации

Формула конвертации:
```
result = (inputValue / rates[baseCurrency]) * rates[targetCurrency]
```

Где `rates` содержит курсы относительно USD (базовая валюта API).

### Хранение данных

Данные сохраняются в Hive:
- **app_settings**: список выбранных валют, последняя базовая валюта, последняя сумма
- **rates_cache**: JSON курсов и время последнего обновления

### Управление состоянием

Используется Riverpod для:
- Отслеживания курсов валют
- Управления базовой валютой
- Хранения суммы для конвертации
- Управления списком выбранных валют

## Разработка

### Добавление новой валюты

Отредактируйте `lib/models/currency_info.dart`:
```dart
'XXX': CurrencyInfo(code: 'XXX', name: 'Currency Name', symbol: 'X'),
```

### Изменение темы

Отредактируйте `lib/theme/app_theme.dart` для изменения цветов и стилей.

## Лицензия

MIT License

